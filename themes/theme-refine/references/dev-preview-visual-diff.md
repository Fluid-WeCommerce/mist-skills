# Visual-parity protocol

The canonical clone/refine protocol is:

[theme-clone/references/dev-preview-visual-diff.md](../../theme-clone/references/dev-preview-visual-diff.md)

Use that file directly. It defines the fresh-machine Mist capabilities,
home/shop/PDP desktop/mobile matrix, exact source and local capture calls,
overlay handling, semantic landmark comparison, strict pass gate, recovery,
and slow-network behavior.

This forwarding file exists only for older links. Do not maintain a second
copy: the previous duplicate drifted into global Playwright installs, a fixed
port, equal-scroll-offset comparisons, and weaker pass semantics.
