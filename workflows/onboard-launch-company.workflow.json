{
  "revision": "2026-07-25.24",
  "slug": "onboard-launch-company",
  "name": "Onboard & Launch Company",
  "description": "Flagship end-to-end onboarding: build a source-of-truth brand and catalog manifest, import the complete product catalog before theme work, clone the home/shop/PDP experience through independently reviewed build stages, and finish with rendered-route and launch-readiness gates. Worker and reviewer models are deliberately cross-vendor so evidence, not one model's self-assessment, decides whether the run passes.",
  "maxParallel": 3,
  "runTitleTemplate": "Onboard {{website_url}}",
  "finalGate": {
    "stepId": "launch-readiness-review",
    "allowNeedsReview": false
  },
  "deriveContext": [
    {
      "set": "build_theme",
      "when": { "in": { "key": "run_scope", "values": ["full", "data_theme", "theme_only"] } }
    },
    { "set": "import_products", "when": { "equals": { "key": "run_scope", "value": "full" } } },
    {
      "set": "push_business_data",
      "when": {
        "in": { "key": "run_scope", "values": ["full", "data_theme", "data_only"] }
      }
    },
    { "set": "discover_ugc", "when": { "includes": { "key": "extras", "value": "discover_ugc" } } },
    {
      "set": "import_brand_social",
      "when": { "includes": { "key": "extras", "value": "import_brand_social" } }
    }
  ],
  "steps": [
    {
      "id": "gather-context",
      "name": "Gather brand & business info",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "continue"
      },
      "skill": "onboarding/onboarding-prefill",
      "target": {
        "type": "manager"
      },
      "acceptance": [
        "The company's website was crawled and the legal/business name, support contact, and business hours (where publicly listed) were located and reported.",
        "Every BBB/Trustpilot value is identity-matched to this exact company: Trustpilot's reviewed registrable domain matches the source domain (allowing www), and BBB carries the exact domain or legal name plus address/phone. Exact profile URLs and match evidence are reported. A namesake profile is discarded as not found. A bot-wall or inaccessible profile is reported as unverified with the attempted URL and exact error — never converted to not found and never newly persisted as a sentinel string such as N/A/Unverified in a rating field. If a stale sentinel predates the run and the documented full-blob update rejects null while omission preserves it, the exact endpoint/status/body is itemized as a platform limitation and downstream steps are told to ignore that field.",
        "Every policy URL was opened and classified from its final same-domain body. Redirect-to-home, a link label without policy text, or one Terms page with no refund/return language is reported as not found and is not pushed as that policy.",
        "Brand identity was harvested from the SOURCE (Step 3f): the palette read out of the site's own stylesheet (`:root` custom properties / dominant hexes, not eyeballed from a screenshot), the real `@font-face` family names + weights with a license verdict on each, the canonical standalone brand logo plus source favicon/icon (not a collaboration, campaign, or generic Fluid default), and 8-12 VERBATIM lines of source copy (headlines, a product description, the primary CTA label). If rendered crawl HTML omitted `<head>`, raw canonical-page HTML was fetched with web_fetch to extract real stylesheet hrefs; CDN filenames were never guessed through search. Stylesheet URLs and the exact token/font evidence used are persisted in STEP_OUTPUT and brand.md Sources so a fresh reviewer can reproduce them.",
        "`brand.md` WAS WRITTEN via the `update_brand_voice` tool (mode `replace`) and exists on disk. This is not optional and STEP_OUTPUT is not a substitute: Mist injects brand.md as a `<brand_voice>` block into every later agent turn for this company, while STEP_OUTPUT reaches only steps that declare a dependsOn, is invisible to QA reviewers, and dies with the run. Verify: `fluid_api(\"/api/settings/brand_guidelines\",\"GET\")` shows a non-null `brand_md`, OR — if only the API push failed — the tool reported a successful local write and the exact API error is recorded in remaining_for_human.",
        "brand.md is SPECIFIC TO THIS BRAND, not a filled-in template. It carries the canonical headings (Brand Overview, Mission & Values, Tone of Voice, Audience, Vocabulary & Naming, Visual Style, Do's and Don'ts, Brands & Sites We Admire, Examples, Sources) and passes the substitution test: no line would read just as true for an unrelated company. Concretely it must contain real hex values, the real font family + weights + license status, the brand's actual price band / market / currency, at least 3 verbatim quoted lines of source copy, a named do/don't list including words this brand never uses, and 3 deliberately OFF-brand example lines. Generic filler ('friendly and approachable', 'modern clean design', 'quality-focused customers') is a FAIL even though the file exists.",
        "Structured brand fields were pushed separately via PATCH /api/settings/brand_guidelines (color, secondary_color, canonical standalone logo_url/icon_url/favicon_url as Fluid-DAM URLs, and every legally re-hostable font as `fonts[]` with required name/file_url plus known file_id/format/weight/style/role). Each asset URL was re-fetched after persistence and returned the expected non-empty media/font bytes. Persistence is retry-safe: an existing valid Fluid-DAM asset proven by brand.md to come from the same canonical source is reused, a missing distinct icon may share the verified logo asset, and a no-source-change retry creates zero duplicate DAM objects. Proprietary or unverified source-font bytes were not copied; their documented substitute was persisted instead. Generic Fluid defaults and collaboration/campaign marks are never accepted as the company's identity. brand.md is the prose layer and does not replace these structured fields.",
        "When context push_business_data is true, whatever onboarding/business fields could be pre-filled via the Fluid API were submitted (HIGH + MEDIUM confidence only) using the EXACT contracts in onboarding/onboarding-prefill/references/api-endpoints.md. Field pushes are BEST-EFFORT: a field whose push still fails after retrying with the documented endpoint/payload (e.g. a support email or rename returning 400) is recorded in STEP_OUTPUT.remaining_for_human with the endpoint + error and does NOT fail this step. When push_business_data is false, business/KYC/onboarding fields were discovered and reported but no company/onboarding mutation was attempted. LOW-confidence fields were always reported but never pushed.",
        "Legal-entity persistence is idempotent: existing entities were listed first and matched by registration number or normalized legal name + country, the matching entity was updated instead of duplicated, and the post-write list contains exactly one matching entity. Ambiguous duplicates are not guessed through and are itemized for a human.",
        "The step's STEP_OUTPUT includes the canonical website_url the rest of the workflow will use, plus a `brand_md` object: { written, path, synced_to_api, sections_filled[], sections_left_as_prompts[], palette{}, fonts[], assets:{logo,icon,favicon}, stylesheet_evidence:[{url,color_tokens,font_faces}], verbatim_quotes, source_urls[] }, reputation:{bbb,trustpilot}, and remaining_for_human[].",
        "HARD-FAIL BAR (the only reasons this step may fail): the website could not be crawled at all, the active company could not be confirmed, NOT A SINGLE field could be pushed, or brand.md was not written / is boilerplate. Individual FIELD failures — however many — are remainders, not failures; a failed root step cascades and kills every downstream step, so reserve failure for total blockage. Note that a brand.md that only failed to SYNC to the API is a remainder, not a failure — the local file is what `<brand_voice>` reads."
      ],
      "dependsOn": []
    },
    {
      "id": "gather-context-qa",
      "name": "QA brand + business data before the build",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop"
      },
      "prompt": "Independently verify and repair the data gather-context produced BEFORE the product import and theme build consume it — a wrong brand color, missing font, wrong market, or a boilerplate brand.md propagates into every theme section, every piece of generated copy, and the store config. Re-fetch from the API and check against the SOURCE site; do not trust gather-context's self-report. Exact brand_guidelines contract (including logo_url, icon_url, favicon_url, fonts, and brand_md; PATCH body wrapped in the brand_guidelines key; asset URLs must be Fluid-DAM hosted): see onboarding/onboarding-prefill/references/api-endpoints.md.\n\n1. Brand palette + type (drives the theme): GET /api/settings/brand_guidelines. Confirm primary/secondary colors are SET (not defaults/placeholders) and match the source site's actual palette. Verify against the SOURCE STYLESHEET, not a screenshot: fetch the site's main CSS bundle and read its `:root` custom properties and `@font-face` blocks. When managed crawl/rendered HTML omits `<head>`, call web_fetch on the canonical homepage, extract real `<link rel=\"stylesheet\">` hrefs from raw HTML, resolve relative URLs, and fetch those bundles. Never Google/Bing a guessed CDN prefix. Persist the exact stylesheet URLs plus the token/font declarations used in STEP_OUTPUT so the later reviewer can reproduce the result. Confirm every source font was IDENTIFIED with a license verdict. For each legally re-hostable font, verify `brand_guidelines.fonts[]` contains the exact `name` + Fluid-DAM `file_url` and the known `file_id`, `format`, `weight`, `style`, and `role`; re-fetch every file_url and reject a 404, empty body, or a single duplicated file masquerading as multiple weights. For proprietary or unverified font bytes, verify they were NOT copied and that brand.md records the original family plus a legally usable substitute; the persisted `fonts[]` entry, if any, must point to the licensed substitute.\n\n2. Brand assets: identify the canonical standalone brand mark from the rendered global header plus source metadata/structured data. Do not accept a collaboration, campaign lockup, or generic Fluid default. Separately identify the source favicon/icon. Upload the verified assets to Fluid DAM, persist logo_url/icon_url/favicon_url, and re-fetch the saved URLs to confirm non-empty media bytes. If no distinct source icon exists, use a verified brand-owned mark and record that fallback; never leave the Fluid default silently.\n\n3. Reputation: independently search BBB by trading name and legal name and Trustpilot by exact registrable domain. Accept values only with the exact profile URL and identity evidence. A bot-wall/403 is `unverified`, not `not found`; keep the attempted URL and exact error in remaining_for_human. Never newly persist sentinel strings such as `N/A`, `Not Found`, or `Unverified` into rating fields. Clear any stale sentinel with the documented full-blob update. If null is rejected and omission preserves the stale value, record the exact endpoint/status/body as a platform limitation, explicitly mark that persisted field unusable downstream, and continue with the verified trust state in STEP_OUTPUT; do not probe guessed endpoints or fail solely because the API cannot clear legacy data.\n\n4. brand.md — THE ARTIFACT EVERY LATER AGENT READS. Read the `<brand_voice>` block in your own context (that IS brand.md) and cross-check with `fluid_api(\"/api/settings/brand_guidelines\",\"GET\")` → `brand_md`. Grade it on SPECIFICITY, not existence, using the substitution test: for each section, ask 'would this sentence be equally true for an unrelated company?'. Check concretely that it contains (a) real hex values matching the stylesheet you just read, (b) the real font family + weights + whether it is licensable, (c) this brand's actual price band, market and currency, (d) at least 3 VERBATIM quoted lines of source copy — spot-check 2 of them by re-fetching the page they came from and confirming the string is really there, (e) a do/don't list naming words this brand never uses, (f) 3 deliberately off-brand example lines, and (g) the stylesheet URLs used for visual tokens. If it is thin or contradicted by the source, REWRITE it yourself via `update_brand_voice` (mode `replace`) and re-verify.\n\n5. Countries/markets (drive currency, locale, tax, theme): GET the company countries and available language settings; confirm the intended market(s), currency, and locale intent, and that brand.md agrees. If an intended locale is inactive, use only a DOCUMENTED write endpoint to activate it. If no documented write contract exists or the documented request fails, record the exact endpoint/status in remaining_for_human and continue: a verified locale intent plus an unsupported activation is a remainder, not a QA failure.\n\n6. Business info (best-effort and scope-aware): where recoverable from the source, verify legal/business name, support contact, and business hours (where public). When context push_business_data is true, confirm recoverable fields landed or record their exact write failure. When it is false, do not mutate company, onboarding, or KYC fields; report what was discovered as deferred-for-human. Missing business info never hard-fails QA.\n\n7. Loop: fix wrong/missing values only when the mutation is in the selected scope and a documented write contract exists; otherwise record the exact remainder. Do not probe guessed PATCH/PUT paths. Do not evaluate or cite this QA step's own live workflow status: it necessarily remains running/rework until your verdict returns, so requiring it to already be passed is circular and impossible.\n\nEnd with STEP_OUTPUT: { brand:{colors,fonts,assets,source_match,stylesheet_evidence}, reputation:{bbb,trustpilot}, brand_md:{present, specific:true|false, sections_filled, verbatim_quotes_verified, rewritten_by_qa:true|false}, countries:[...], business:{...}|'deferred', status:'pass'|'cap-reached', remaining_for_human:[...] }.",
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "Brand colors + fonts were verified against the SOURCE STYLESHEET (its `:root` tokens and `@font-face` blocks), not merely 'set' and not eyeballed from a screenshot. Exact stylesheet URLs and token/font declarations are persisted in STEP_OUTPUT. Legally re-hostable fonts are present in GET /api/settings/brand_guidelines → fonts[] with exact DAM URLs and metadata, and every URL was re-fetched successfully; proprietary/unverified bytes are absent and a licensed substitute is documented. A fixable palette/font mismatch was fixed and re-verified.",
        "logo_url, icon_url, and favicon_url resolve to non-empty Fluid-DAM assets verified as this company's canonical standalone identity. Collaboration/campaign marks and generic Fluid defaults were replaced. A documented same-brand fallback is allowed only when the source has no distinct icon. Existing correct DAM assets were reused, and a no-source-change retry created zero duplicate brand assets.",
        "brand.md was read and graded on SPECIFICITY via the substitution test — not merely confirmed to exist. The QA reports which required specifics are present (real hexes matching the stylesheet, real font family + weights + license verdict, price band / market / currency, >=3 verbatim source quotes, a do/don't list with words this brand never uses, 3 off-brand examples) and re-verified at least 2 quoted lines against the live source page.",
        "If brand.md was missing, boilerplate, or contradicted the source, QA REWROTE it via update_brand_voice (mode `replace`) and re-verified. Reporting it as a remainder without fixing it does not satisfy this step — every downstream agent reads this file.",
        "The intended market country/countries, currency, and locale intent were verified, and brand.md agrees. Recoverable configuration mismatches were fixed only through documented write contracts; an inactive locale with no working documented write contract is itemized in remaining_for_human and does not fail this step.",
        "Brand + countries correctness is confirmed, and business fields (legal/business name, support contact, business hours) were verified where recoverable from the source OR explicitly marked deferred-for-human — missing business info is a remainder, never a hard blocker.",
        "Third-party trust data was independently entity-matched before acceptance: every Trustpilot score uses a reviewed domain equal to the source company's registrable domain (allowing www), and every BBB profile matches the exact domain or legal name plus address/phone. Namesake-company ratings were removed and reported as not found. Bot-walled/inaccessible profiles are reported as unverified with the attempted URL/error, and no sentinel string such as N/A/Not Found/Unverified is newly persisted as a rating. A legacy sentinel that the documented API provably cannot clear is explicitly marked unusable downstream with the exact null/omission failures in remaining_for_human and does not alone fail this step.",
        "Terms, privacy, refund, and shipping policy claims were checked against each final same-domain page body. A redirect, link label, or page without the claimed policy language was removed rather than accepted as an OK policy.",
        "STEP_OUTPUT reports status 'pass' or 'cap-reached' with an itemized remaining_for_human list and the brand_md verdict — no silent pass.",
        "HARD-FAIL BAR: grading is against OUTCOMES AVAILABLE DOWNSTREAM, not push mechanics. A value that failed to persist via settings but is correctly RECORDED in STEP_OUTPUT for downstream steps (e.g. brand color the theme build applies directly, or verified locale intent whose documented activation endpoint is unavailable) plus listed in remaining_for_human is acceptable — do not fail the run over a field-level 4xx or missing write capability. A brand.md that is present and specific LOCALLY but failed to sync to the API is a PASS with a remainder — the local file is what `<brand_voice>` injects. Never fail because this step's own workflow status is still running/rework. FAIL only when the brand palette is wrong/unverified against the source AND unrecoverable this turn, the intended market cannot be determined, or brand.md is still absent/boilerplate after QA's own rewrite attempt."
      ],
      "dependsOn": ["gather-context"]
    },
    {
      "id": "catalog-source-manifest",
      "name": "Build and reconcile the complete source catalog manifest",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop"
      },
      "prompt": "Build the complete product SOURCE MANIFEST before any product write. Call run_skill(\"fluid-product-admin-import\") and follow its Catalog completion contract. This step is read-only against Fluid.\n\nDiscovery is a union: (1) parse sitemap.xml plus every child sitemap and retain every product URL, lastmod, and sitemap image; (2) exhaust any structured catalog API until its documented empty-page/cursor terminator; (3) exhaust EVERY discovered collection/category pagination — never a top-N sample — and collect product links; (4) fetch every unique PDP, including an advertised `.md` twin, rendered HTML/JSON-LD, embedded application state, and structured data as needed. A fixed batch cap such as 100 is transport chunking, never a completion limit. A collection that ended in a retry-exhaustion/network error is NOT exhausted: retry it independently, and if it still cannot be enumerated report it as unresolved so the step fails rather than calling discovery complete.\n\nFor each discovered URL record immutable source_id/url/handle, requested and final URL, HTTP status/content type, title, exact price/currency, ALL source image URLs available from the sitemap, structured data, embedded state, and rendered PDP, exact option axes and REAL variant combinations/count, description, and evidence method. One nonempty image is not proof of a complete gallery: normalize/deduplicate every gallery image from JSON-LD, embedded product state, src/srcset/data-src attributes, thumbnails, and structured APIs, excluding unrelated recommendations/chrome. Likewise, never manufacture variants as a Cartesian product unless the source explicitly proves every combination exists. A 200 that redirects to the homepage is stale, not a live product. Markdown is copy/data evidence only: fill missing images/options from sitemap images, JSON-LD, embedded state, rendered HTML, or APIs. Never exclude a product because one parser failed.\n\nWrite source-catalog.json to a fresh run-specific working directory and checkpoint every successfully resolved product. Reconcile all discovery sets. Every discovered URL must be either a live product or an explicit exclusion with objective evidence. Report duplicate-title groups because later idempotency must use immutable source identity, never title-only fallback. Compute a SHA-256 of the final manifest.\n\nBefore finalizing, independently open a deterministic sample of at least 10 live PDPs that includes the selected flagship/most-complex PDP, the most-expensive item, a one-variant item, and multiple multi-variant items. For each, compare the manifest's exact gallery image count/URLs and variant count/options to the rendered/embedded source evidence. Store this compact `fidelity_sample` in the manifest and STEP_OUTPUT; a mismatch is repair work, not a note. Also report the whole-manifest image-count distribution so a systemic one-image fallback is visible to QA.\n\nUse at most 10 concurrent source fetches — the implementation's concurrency constant must be <=10 — with bounded retries, exponential backoff/jitter, and resume from checkpoints after interruption. Mist run_cli calls have a 60-second execution window: make each invocation process an explicit bounded chunk that completes and checkpoints within that window; do not depend on SIGTERM as normal pagination. Keep large manifests on disk. Never print or read the complete manifest into chat/model context; inspect it with targeted jq/Node summaries and report only compact counts, hashes, samples, and unresolved identities. End with STEP_OUTPUT: { manifest_path, manifest_sha256, observed_at, discovery:{sitemap,api,collections,unique,collection_errors}, live_count, excluded_count, exclusions_by_reason, unresolved_count, duplicate_title_groups, missing_fields:{title,price,images,variants}, image_count_distribution, fidelity_sample, completion_proof }.",
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "source-catalog.json exists and its SHA-256, observation time, and path are reported.",
        "Discovery used the union of sitemap(s), structured catalog endpoints when available, and collection/category links; every source pagination/batch exit condition is recorded. Every discovered collection/category reached a content-based terminal page/cursor with `collection_errors: 0`; bounded retry exhaustion is unresolved work, not a successful exit condition.",
        "Every unique discovered product URL is accounted for as one live product or an evidence-backed exclusion. Redirect-to-home, parser failures, bot blocks, missing images, and batch caps are not silently counted as complete products or used as permission to stop.",
        "Every live record has an immutable source identity plus source/final URL, status, title, exact price/currency, ALL normalized gallery images, option axes, real variant combinations/count, and extraction evidence; missing fields are zero or itemized as unresolved. A single sitemap/OG image does not count as a complete gallery when rendered or embedded source evidence exposes more media, and variants were not invented as an unproven Cartesian product.",
        "A deterministic fidelity_sample of at least 10 live PDPs includes the flagship/most-complex PDP, most-expensive item, one single-variant item, and multiple multi-variant items. For every sample, the manifest's exact gallery count/URLs and variant count/options match independently re-opened rendered/embedded source evidence. The whole-manifest image-count distribution is reported so systemic one-image fallback is reviewable.",
        "Duplicate-title groups are reported, and the manifest does not deduplicate distinct source handles by title.",
        "The worker exhausted every discovered collection/category (not a top-N sample), used a concurrency setting <=10, completed each run_cli chunk inside Mist's 60-second window, and kept the full manifest on disk instead of returning it as a tool/chat output.",
        "HARD-FAIL BAR: fail when unresolved_count is nonzero, any collection/category remains on a terminal fetch error, discovery was capped before exhaustion, the fidelity sample has an image/variant mismatch, the manifest does not reconcile every discovered URL, or the file/evidence is missing."
      ],
      "dependsOn": ["gather-context-qa"],
      "runIf": {
        "flag": "import_products"
      }
    },
    {
      "id": "products-import",
      "name": "Import products (Connect / scrape / manual)",
      "model": "anthropic/claude-opus-5",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "stop"
      },
      "prompt": "First call `run_skill(\"fluid-product-admin-import\")` and read the loaded skill plus every referenced file. Then import the complete product catalog so the destination matches the already-reviewed source manifest; treat the loaded contract as authoritative instead of inventing endpoint shapes.\n\nINPUT INVARIANT: read the exact `manifest_path` and `manifest_sha256` from `catalog-source-manifest`, recompute the SHA-256, and stop on a mismatch. Do not replace the approved denominator with a smaller collection list or a fresh ad-hoc crawl.\n\nBefore the first Fluid catalog call, use `query_docs` against `/openapi/api-reference/storefront-v2026-04.yaml` to inspect the documented Product list/create/update schemas. Use only `/api/v202604/company/products` for product CRUD. Cursor-paginate destination lists with `page[limit]=100` and `meta.pagination.next_cursor` until it is null; do not expect `total_count`.\n\nBranch on `products_source`:\n- `connect`: verify the selected droplet is connected, run its documented product-sync action, wait for it to settle, then reconcile every manifest identity and field. A successful sync is not proof of fidelity.\n- `scrape`: create/update from the manifest with immutable source identity checkpoints. Use the documented raw ProductWrite lifecycle `status:\"active\"` + `public:true`; nested `images_attributes[].image_url`; exact option axes and all variants; and `variant_countries_attributes` containing integer `country_id`, `active`, and decimal-string prices. Do not send `currency_code` in the v202604 write—the selected company country determines it. Verify response `pricing.currency_code` against the source.\n- `manual`: create only the explicit Sample Product stub and report the full human-entry remainder. Never present this branch as a completed catalog.\n\nFor remote media, ingest it into Fluid DAM without hotlinking: use `dam_upload` for a sandbox file or `fluid dam upload --url <SOURCE_URL>` for server-side `external_asset_url` ingestion. Persist source URL → `asset.default_variant_url` mappings. Never leave a real source product on a placeholder.\n\nIdentity is source ID/canonical source URL, never title. Persist the returned Fluid ID, slug, and canonical URL after every successful write before starting the next item. On restart, verify checkpoints and resume. Ambiguous recovery matches stop for review; distinct duplicate titles stay distinct.\n\nAfter writes, cursor-paginate the entire destination, GET detail records as needed, and reconcile every live manifest item: title, exact source description, price/currency, all available images, option names/values, variant count, lifecycle, and membership. Fix every recoverable mismatch and run a second idempotency pass that creates no duplicates.\n\nEnd with STEP_OUTPUT: { products_source, manifest_path, manifest_sha256, source_count, imported_count, destination_pages, coverage_pct, status_counts, sample:[...], match_rates:{title,price,currency,images,variants,description}, missing_from_fluid:[], duplicate_title_groups, placeholder_only:[], failures:[], checkpoint_path, idempotent_second_pass, remaining_for_human:[] }.",
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "A SOURCE MANIFEST was built before importing (Cases A/B): the source catalog was enumerated to a concrete list of {source_url, title, price, currency, images, option axes, variant count} with app-helper/$0 junk filtered out, `source_count` is reported from it, and the enumeration method + exit condition are recorded (all sitemap/collection pages exhausted, API pagination returned empty, or equivalent). 'Products exist in Fluid' proves nothing without a demonstrably complete source number.",
        "The importer consumed the exact source-catalog.json path + SHA-256 produced by catalog-source-manifest; it did not replace the reviewed denominator with a smaller collection-page list or a new ad-hoc crawl.",
        "API CONTRACT: product CRUD used the documented `/api/v202604/company/products` surface after a query_docs schema lookup. Destination enumeration followed every `meta.pagination.next_cursor` to null; it did not use a legacy product CRUD fallback or assume `total_count` exists.",
        "COVERAGE: the imported catalog is 100% of the filtered source manifest and `missing_from_fluid` is empty. A source item may be excluded only when it is recorded in `junk_filtered` with objective evidence that it is not a sellable catalog product. Bot blocks, API errors, and partial pagination are recovery work, not permission to call a partial catalog complete.",
        "IDENTITY: every live source_id maps to one distinct Fluid product ID in a persisted checkpoint, no two source identities map to the same Fluid ID, and duplicate source titles were not collapsed by title-only fallback. Each successful create was checkpointed before the next item so restart/resume is idempotent.",
        "TITLES + PRICES match the source: on a random sample of >=10 imported products (or all, if fewer) that INCLUDES the flagship/most-expensive product and one multi-variant product, each title matches its source record and the price + currency match the source exactly on the default country's `variant_countries` pricing. Non-zero is not the bar — CORRECT is the bar.",
        "IMAGES match the source: sampled products each have >=1 real image hosted on the Fluid DAM. A product on Fluid's grey placeholder (`images: []` / `image_url` ending `/placeholders/image.jpg`) or hotlinking the source CDN counts as NOT imported for that product. Report the count of placeholder-only products across the WHOLE catalog, not just the sample — it must be 0, or every exception named with a reason.",
        "VARIANTS match the source: where the source PDP offers colours / sizes / any option axis, the Fluid product has the same option names and values and one variant per real combination. A whole catalog sitting at exactly one master variant per product, when the source has colourways or sizes, is silent data loss and fails this criterion — it also guarantees the later add-to-cart QA finds every PDP non-variant-aware.",
        "Imported products resolve as raw `status: \"active\"` and `active: true`; writes used `public: true`, decimal-string country prices, and no unsupported `currency_code` field. Descriptions come from the source, no duplicate slugs were created, and no source-absent subscription plan was attached.",
        "SUBSCRIPTION DEFAULT SAFETY: when the source product has no subscription offer, create used the non-empty v202604 skip sentinel `product_subscription_plans_attributes:[{\"_destroy\":true}]`; omission and an empty array are forbidden because they attach the company-default plan. A complete destination re-read proves `has_subscription_plans:false` and zero active/default subscription joins for every such product, with any historical joins repaired by returned join ID rather than disabling the company-wide plan.",
        "Mismatches that were repairable from the manifest (missing images, collapsed variants, wrong price) were FIXED and re-verified during this step, not deferred to the QA steps. STEP_OUTPUT reports source_count, imported_count, coverage_pct, the per-product sample table, and per-field match_rates — spot-checkable numbers, not a self-assessment.",
        "For Case C: the report explicitly says 'MANUAL ENTRY REQUIRED' and names how many products need human entry; the stub is the only product created.",
        "HARD-FAIL BAR: for Cases A/B, fail when the source manifest is missing/incomplete, coverage is below 100%, a sellable source product is missing, or the sample shows systemic drift — $0/placeholder-priced shells, placeholder images across the catalog, collapsed source variants, or draft products. Case C remains an explicit manual-entry outcome and must never be presented as a completed catalog."
      ],
      "dependsOn": ["catalog-source-manifest"],
      "runIf": {
        "flag": "import_products"
      }
    },
    {
      "id": "theme-scrape-inventory",
      "name": "Theme 1/6 — scrape source + section inventory + DAM assets",
      "model": "google/gemini-3.6-flash",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop",
        "requiredTools": [
          {
            "tool": "read_file",
            "input": {
              "path": "clone-manifest.json"
            }
          },
          {
            "tool": "view_project_image",
            "minSuccessfulCalls": 6,
            "distinctBy": ["path"]
          }
        ]
      },
      "prompt": "First theme step. You are in a scaffolded Fluid base-theme project. Call run_skill(\"themes/theme-clone\") and execute its discovery phases only: page discovery, content scraping, rendered visual audit, source CSS/token extraction, and DAM asset upload. Do not build sections or templates yet.\n\nBuild three evidence layers instead of treating one crawl payload as truth:\n1. CONTENT/DATA — use AI-friendly Markdown when advertised plus sitemap/collection/PDP content for exact copy and catalog facts. Markdown is not visual truth.\n2. DOM/TOKENS — retain rendered HTML with global chrome (`only_main_content:false`) and inspect linked stylesheets, CSS custom properties, @font-face declarations, and layout geometry.\n3. VISUAL — follow themes/theme-clone/references/dev-preview-visual-diff.md. In Mist, use managed `crawl` with `screenshot_options:{full_page:true,quality:90,viewport:{width,height}}`; capture home, shop, and PDP at 1440x900 and 390x844. This must work without local/global Playwright. Inspect HTML before using a constrained click action to dismiss a geo/cookie/newsletter overlay, then record the selector/action. A redirected, blank, loading, or obscured screenshot is invalid. For every capture, copy the crawl result's `<!-- screenshot baseline -->` object into the matching manifest cell. It must be the real `.mist-desktop/source-baselines/` file plus captured_at, sha256, positive bytes, decoded width/height, requested_viewport, final_url, status, and overlay_handling. A hosted URL, chat attachment id, prose claim, or label such as `crawl:1440x900` is NOT evidence. Verify each file with `run_cli({command:\"git\",args:[\"hash-object\",\"--no-filters\",\"--\",\"<relative-path>\"]})` and compare the raw-byte digest before claiming 6/6.\n\nPriority routes are the homepage, the source's primary shop/collection index, and the flagship or most complex multi-variant PDP. Inventory EVERY visible section on all three in source order, plus global header/footer. Inspect up to two more PDPs and four key static pages for content/structure coverage. Ingest every asset required by the priority routes with Mist's `dam_upload`: pass a public source directly as `url` so the upload service uses `external_asset_url`, and use `path` only for an existing sandbox file. If and only if a direct upload returns a size rejection, call `compress_media(url=...)` and immediately retry with `dam_upload(path=<output_path>)`; never omit or replace media because it is large. Record `asset.default_variant_url`. Extract real font families/weights and a license/substitution decision. The source wins any disagreement with gather-context, but record the conflict.\n\nWrite `clone-manifest.json` at the theme root with: source_url, page_manifest, section_inventory for home/shop/PDP/header/footer, brand_tokens, dam_assets, and `visual_routes` entries for home/shop/pdp. Each visual route contains source_url, future built_path, ordered landmarks `{id,source_selector,built_selector}` for every visible priority section, and `source_evidence.desktop` + `source_evidence.mobile` objects with the required durable fields above. Also record `hero_copy.headline`, `hero_copy.subheadline`, and `hero_copy.primary_cta` verbatim, each with its evidence source (`markdown`, `rendered_html`, or `screenshot`). Markdown and a rendered screenshot are complementary: dynamic overlay text can be absent from Markdown, while a screenshot may show only one carousel state. If no evidence establishes a field, store null with the reason; never substitute invented or base-theme copy. Built selectors use stable template section ids chosen now so later build steps implement the manifest rather than inventing mappings afterward.\n\nThese are local QA artifacts, not Fluid theme resources. Create or update `.fluidignore` at the theme root so it includes exact entries for `clone-manifest.json`, `.mist-desktop/source-baselines/`, and `baselines/` while preserving every existing rule. Verify the ignore file before starting the dev watcher; otherwise theme dev will repeatedly send invalid resource uploads and flood the user with 404 errors.\n\nEnd with STEP_OUTPUT: manifest path, priority route URLs, per-route landmark counts, 6/6 verified local source baseline paths + hashes, verbatim hero copy + evidence sources, page count, uploaded asset count, brand tokens, and non-priority gaps.",
      "target": {
        "type": "kind",
        "kind": "theme"
      },
      "maxReworkRounds": 1,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "DISCOVERY-ONLY BOUNDARY: this step does not build the theme. The local preview may still be the generic scaffold, and future built_path routes/selectors are plans that are verified in later build steps. Do not fail this step because those routes do not yet resolve or because the scaffold does not match the source.",
        "`clone-manifest.json` exists at the theme project root and contains a page manifest, home + shop + representative-PDP section inventories with concrete computed style values, header/footer inventory, brand tokens (colors + fonts with licensing notes), and a source-URL → DAM-URL asset map — verify by READING the file, not the worker's claim.",
        "The theme root `.fluidignore` preserves its prior rules and contains exact entries for `clone-manifest.json`, `.mist-desktop/source-baselines/`, and `baselines/`, so the theme dev watcher cannot upload local discovery/visual-QA artifacts as Fluid theme resources.",
        "The manifest has `visual_routes.home`, `visual_routes.shop`, and `visual_routes.pdp`. Each records source_url, built_path, and an ordered landmark list mapping every visible priority source section selector to a stable built section id/selector.",
        "Source discovery used three distinct evidence layers: AI-friendly Markdown for copy/catalog facts, rendered HTML/CSS for DOM/tokens/fonts, and clean managed-browser full-page screenshots at 1440x900 and 390x844 for visual structure. The source matrix is 6/6 (home/shop/PDP × desktop/mobile). Each `source_evidence` cell records a real local `.mist-desktop/source-baselines/` path, captured_at, 64-character sha256, positive bytes, decoded width/height, requested_viewport, final_url, HTTP status, and overlay_handling; the QA reviewer verifies every file with `git hash-object --no-filters -- <path>` and rejects hosted URLs, chat ids, prose claims, and opaque `crawl:<viewport>` labels.",
        "`hero_copy` records the source homepage headline, subheadline, and primary CTA verbatim. Each field cites every applicable evidence layer (Markdown, rendered HTML, screenshot), not a single opaque label. A field absent from all evidence is null with a reason; invented or base-theme replacement copy is a hard failure.",
        "`priority_media` reconciles every priority-route `<img>`, `<video>`, `<source>`, poster, and CSS media URL found in the retained rendered HTML at desktop and mobile. Each entry records source URL, route/landmark/viewport role, media kind, playback attributes, upload attempt, returned `asset.default_variant_url`, and compression result when applicable. A generic note such as `media: video` without the exact source and DAM mapping is incomplete.",
        "Every required self-hosted priority video was attempted with `dam_upload(url=..., create_media=true)`. A size rejection was followed by `compress_media(url=...)` and an immediate `dam_upload(path=<output_path>, create_media=true)` retry before the temporary one-hour compressed handoff expired; it was never treated as permission to omit or replace the video. Spot-check each uploaded priority video: its DAM URL returns HTTP 200 with a video content type. If the source truly has zero priority videos, the manifest says so with rendered-HTML evidence.",
        "Spot-check 2-3 DAM URLs from the manifest: they resolve to real images (HTTP 200).",
        "The section inventory covers every visible home, shop, and representative-PDP section in source order (compare against the live rendered pages), including promotional/editorial modules below the main collection grid or product detail, cross-sells, related-product rails, collection exploration, and media/video blocks.",
        "For all three priority routes the manifest contains auditable references to the captured Markdown (when advertised), retained rendered HTML/CSS evidence, and both durable screenshot files; screenshots alone are not proof of exact copy or DOM/token extraction.",
        "HARD-FAIL BAR: fail when any priority route could not be captured cleanly, a priority landmark inventory is incomplete, the manifest is missing/empty, zero required images landed on the DAM, any priority video source is missing from `priority_media` or lacks a successful DAM mapping/structured failed attempt, or hero copy was invented rather than captured-or-recorded-as-missing. Non-priority static-page gaps can remain itemized notes."
      ],
      "dependsOn": ["gather-context-qa"],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "theme-tokens-skeleton",
      "name": "Theme 2/6 — design tokens + skeleton boots",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop",
        "requiredTools": [
          {
            "tool": "screenshot_preview"
          },
          {
            "tool": "read_preview_dom"
          }
        ]
      },
      "prompt": "Wire the source site's design tokens into the theme so every later section references them. Call run_skill(\"themes/theme-clone\") and execute ONLY Phase 4b (Theme Config Setup) using `clone-manifest.json`'s brand_tokens:\n\n1. `config/settings_data.json`: set the 12-color palette + 5-font palette + heading sizes from the manifest (every font slot seeded — defaults in settings_schema.json do NOT auto-apply to missing keys).\n2. `config/settings_schema.json`: confirm every color/font setting carries its `option_group` (background_colors / font_families) — sections' dropdowns are empty without them.\n3. `layouts/theme.liquid`: all 12 `--clr-*` and 5 `--ff-*` CSS variables wired in :root with `| default:` fallbacks. If the source font is importable, add the @font-face (note the licensing disclaimer in a comment); otherwise use the manifest's chosen substitute and note it.\n4. Honor the caller context's `theme_target`: `new` → this scaffolded project is the target; `existing` with `theme_id` → record that id as the push target for later steps. Record the decision in clone-manifest.json under `theme_target_resolved`.\n5. Call `start_preview` to boot the long-lived dev server with Mist's bundled CLI and port manager; never run a dev server through bounded `run_cli`. Call `preview_state`, `read_local_server_logs`, `read_preview_console`, and `screenshot_preview` to confirm the skeleton renders (base-theme placeholder content is fine at this point).\n\nEnd with STEP_OUTPUT: tokens set (colors/fonts), font licensing decision, theme_target_resolved, local URL/port, and preview/log status.",
      "target": {
        "type": "kind",
        "kind": "theme"
      },
      "maxReworkRounds": 1,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "settings_data.json + settings_schema.json parse as valid JSON; the 12 color slots and 5 font slots are populated with the manifest's brand values (not base-theme defaults), and every color/font setting has its option_group.",
        "layouts/theme.liquid wires all --clr-* and --ff-* variables with | default: fallbacks; the font decision (real @font-face or named substitute) is recorded.",
        "The dev preview boots and renders without server errors — verified with a screenshot_preview this turn (placeholder content is acceptable; this step is about tokens + skeleton, not content).",
        "HARD-FAIL BAR: fail only when a config file is invalid/unwritten, tokens still hold base-theme defaults, or the dev preview cannot boot. Imperfect color mapping or a debatable font substitute is a NOTE for the refine pass."
      ],
      "dependsOn": ["theme-scrape-inventory"],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "theme-header-footer",
      "name": "Theme 3/6 — header, navigation & footer",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop",
        "requiredTools": [
          {
            "tool": "screenshot_preview",
            "minSuccessfulCalls": 2,
            "distinctBy": ["width"]
          },
          {
            "tool": "interact_preview"
          },
          {
            "tool": "read_preview_dom"
          }
        ]
      },
      "prompt": "Build the global chrome: `sections/main_navbar` and `sections/main_footer` matched to the source site's header/footer using clone-manifest.json (inventory + DAM assets). Call run_skill(\"themes/theme-clone\") for the section architecture rules (Section Shell + Container, blocks for content, tokens not hex, canonical blocks/components) and use the exact materialized `<THEME_AUDIT_PATH>` it returns; a fresh theme does not contain the skill's script.\n\nRequirements:\n- Navbar: source's logo (DAM URL), nav items mirroring the source's top-level navigation but pointing at FLUID routes (/home, /home/collections/<slug>, /home/pages/<slug> …); working mobile hamburger; PRESERVE Fluid's JS hooks verbatim (#show-cart, #fluid-cart-count, #show-language-country-dropdown, .saveLocaleBtn, .country-selector, .language-selector — the canonical navbar components own these; render them, never rewrite).\n- Footer: source's column structure, social links, legal links, newsletter block if the source has one.\n- Scope boundary: edit ONLY `sections/main_navbar`, `sections/main_footer`, and directly required navbar/footer blocks/components/assets. Do not purge or rewrite placeholder content in unrelated homepage/PDP/collection sections; those surfaces deliberately remain a skeleton until their later workflow steps own them. Placeholder copy inside the navbar/footer you touched is still a defect.\n- Audit gate: run `python3 <THEME_AUDIT_PATH>` on both section files and fix EVERY violation before finishing. Do not modify the materialized audit script in this step.\n- Verify in the dev preview with screenshot_preview at desktop AND mobile widths. Interact with the real hamburger and capture the open state; a CSS-only fake state does not prove the JS interaction. After the click, inspect the rendered DOM: the logo must be complete with positive naturalWidth/naturalHeight, `aria-expanded` must be true, exactly one visible primary mobile navigation list may exist, and every expected top-level label appears exactly once. A component that already renders the links must not also receive a JS-cloned copy.\n\nEnd with STEP_OUTPUT: sections written, audit result, nav items mapped (label → route), mobile DOM counts/logo dimensions, and any deviations from the source recorded as notes for the refine pass.",
      "target": {
        "type": "kind",
        "kind": "theme"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "main_navbar and main_footer exist, render in the dev preview without Liquid errors (check the dev server log), and visibly correspond to the source's header/footer structure (logo, nav items, footer columns). The rendered source logo is complete with positive naturalWidth/naturalHeight rather than an alt-text/broken-image fallback.",
        "Fluid's cart/locale JS hooks are intact in the navbar (grep for #show-cart, #fluid-cart-count — their absence breaks cart + locale switching store-wide).",
        "The worker ran the exact materialized `<THEME_AUDIT_PATH>` returned by run_skill on both section files without modifying the audit asset; the independent QA reviewer runs its available read-only `fluid theme lint --json`, gets 0 errors, and directly reads/searches both files for the required hooks and malformed schema tails.",
        "Fresh mobile-width screenshots show both collapsed and real clicked-open hamburger states, with the expected links visible exactly once and no horizontal bleed. The clicked state has `aria-expanded=true`, exactly one visible primary mobile navigation list, and no duplicate label sequence caused by rendering a menu and cloning it again in JavaScript. A CSS-only or `:target` state does not substitute for the actual JS interaction.",
        "Authored NAVBAR/FOOTER copy obeys `brand.md`. Mist injects the company's brand guide into every turn as a `<brand_voice>` block; scraped source strings win, and any navbar/footer string the agent had to write is in that voice. Placeholder copy inside the touched navbar/footer is a defect. Do NOT fail this step for placeholder copy in unrelated page-body sections: Theme 4/5 owns those skeleton surfaces, and editing them here violates the step boundary.",
        "Scope stayed inside `sections/main_navbar`, `sections/main_footer`, and their directly required dependencies. The worker did not rewrite unrelated homepage/PDP/collection sections or the shared audit script to satisfy this chrome-only step.",
        "HARD-FAIL BAR: fail only for Liquid errors, missing sections, destroyed JS hooks, or unfixed audit violations. Pixel-level spacing/typography deltas vs the source are NOTES for the refine pass — record them, do not fail."
      ],
      "dependsOn": ["theme-tokens-skeleton"],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "theme-homepage",
      "name": "Theme 4/6 — homepage sections",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop",
        "requiredTools": [
          {
            "tool": "view_project_image",
            "minSuccessfulCalls": 2,
            "distinctBy": ["path"]
          },
          {
            "tool": "screenshot_preview",
            "minSuccessfulCalls": 2,
            "distinctBy": ["width"]
          },
          {
            "tool": "read_preview_dom"
          }
        ]
      },
      "prompt": "Build the homepage. Using clone-manifest.json's homepage section inventory, compose `home_page/default/index.liquid` from the base theme's canonical section library (heroes, image_text_split, feature_grid, featured_products, testimonial_grid, faq_accordion, cta_banner_v2 …) — call run_skill(\"themes/theme-clone\") for the library list, section rules, and exact materialized `<THEME_AUDIT_PATH>`. Prefer canonical sections; only write a custom section when nothing in the library fits, following the full Section Shell + Container pattern.\n\nRequirements:\n- If a homepage section needs a collection to render, inspect the current schema with query_docs and use `/api/v202604/company/collections`. GET-before-create; persist the returned ID/canonical URL. Set complete membership with the collection's `product_ids` full-replacement field, or patch a product's `collection_ids` through `/api/v202604/company/products/{id}`. v202604 PATCH is partial and does NOT require resending title. The later content-import step reconciles the full source set; do not wait for it.\n- Every source homepage section is represented, in source order, with REAL scraped copy and DAM image URLs from the manifest — no lorem ipsum, no hotlinked source-CDN images.\n- Content lives in blocks (richtext for text, canonical image blocks for images); template schema carries sections + order + settings ONLY (never blocks data).\n- Tokens, not hex: colors via the background_colors option group, fonts via font_families.\n- Audit gate: `python3 <THEME_AUDIT_PATH>` on every touched file — fix every violation. A fresh theme does not contain the skill's script; never assume `scripts/theme_audit.py` exists.\n- Verify with screenshot_preview at desktop and mobile; compare against the manifest's source notes and record deviations as refine notes.\n\nEnd with STEP_OUTPUT: sections used (canonical vs custom), audit result, and the deviation notes list for the refine pass.",
      "target": {
        "type": "kind",
        "kind": "theme"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "The reviewer opened the durable rendered SOURCE screenshot itself and compared it against the clone side by side. A worker description, DOM inventory, or metrics-only review does not satisfy this criterion.",
        "The homepage renders in the dev preview without server/Liquid errors and contains a section for every entry in the manifest's homepage inventory, in source order (verify against the manifest, not the worker's claim).",
        "The homepage's hero headline, subheadline and primary CTA label match `clone-manifest.json`'s `hero_copy` VERBATIM — quote all three in STEP_OUTPUT beside the manifest's values. Where the manifest recorded a null, the step must say what it used instead and why. Sections otherwise use real scraped copy + DAM images: no base-theme boilerplate anywhere on the page (\"Your Logo\", \"Built different\", \"NEW DROP\", \"Trusted by thousands\", \"Shop the drop\" and friends), and no <img> pointing at the source site's CDN.",
        "Every homepage landmark recorded as video renders a real `<video>` using the matching DAM URL(s), including separate desktop/mobile sources when the manifest found them, with source-matching autoplay/loop/muted/playsinline behavior and a poster fallback. `read_preview_dom` verifies those elements and URLs. A still image, poster-only render, gradient, or flat-color substitute is a hard failure.",
        "The exact materialized `<THEME_AUDIT_PATH>` returned by run_skill passes on the homepage template and every new/modified section file.",
        "Desktop + mobile screenshots were taken this turn and the page is structurally coherent at both widths (no blank page, no stacked garbage).",
        "Authored copy obeys `brand.md`. Mist injects the company's brand guide into every turn as a `<brand_voice>` block; scraped source strings win, and any string the agent had to write itself is in that voice and consistent with its Do's and Don'ts. Surviving base-theme placeholder copy — \"NEW DROP\", \"Built different\", \"Trusted by thousands\", \"Shop the drop\", lorem — is a defect even when the layout matches, and is the single most visible way a clone reads as generic.",
        "HARD-FAIL BAR: fail for a broken/blank homepage, a missing majority of sections, any missing/substituted priority video, any base-theme placeholder copy surviving in a visible section, hero copy that does not match the manifest, authored copy that violates brand.md, or unfixed audit violations. Spacing/size/color drift vs the source remains a refine-pass note."
      ],
      "dependsOn": ["theme-header-footer"],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "theme-product-collection",
      "name": "Theme 5/6 — product & collection templates",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop",
        "requiredTools": [
          {
            "tool": "view_project_image",
            "minSuccessfulCalls": 4,
            "distinctBy": ["path"]
          },
          {
            "tool": "screenshot_preview",
            "minSuccessfulCalls": 4,
            "distinctBy": ["path", "width"]
          },
          {
            "tool": "read_preview_dom",
            "minSuccessfulCalls": 2,
            "distinctBy": ["path"]
          }
        ]
      },
      "prompt": "Build the product and collection surfaces — the catalog was imported BEFORE the theme track, so verify against REAL products. Call run_skill(\"themes/theme-clone\") for template rules (Phase 5), the canonical PDP/collection section list, and the exact materialized `<THEME_AUDIT_PATH>`.\n\nRequirements:\n- `product/default/index.liquid`: lead with the canonical product-data section that wires Fluid's product object + add-to-cart (`product_hero` in the current base theme; `main_product` in older scaffolds — use whichever the scaffold ships, NEVER a hand-rolled replacement and never modify `sections/main_product`), then match the SOURCE PDP's section structure from the manifest (benefits, ingredients, how-to-use, reviews, related — whatever the source uses) with canonical product_* sections. This one default template drives every PDP.\n- `collection/default/index.liquid` (+ `collection_index` if the source has a collections/shop index) using canonical collection sections. Remember: Liquid's `collections` global has a different shape than the REST API (see the skill's gotchas).\n- Verify in the dev preview with REAL data: cursor-paginate `GET /api/v202604/company/products?page[limit]=100` through `meta.pagination.next_cursor`, then open the flagship or most complex real product's local PDP route plus the primary collection route and screenshot both at desktop and mobile. Use canonical URLs/paths returned by the API and mapped into the local preview—never compose a slug. If the run has no real products, verify the placeholder/empty state and report that the flagship launch gate remains unmet.\n- Audit gate: `python3 <THEME_AUDIT_PATH>` on all touched files. A fresh theme does not contain the skill's script.\n\nEnd with STEP_OUTPUT: templates written, which real product/collection routes were verified with screenshots, audit result, refine notes.",
      "target": {
        "type": "kind",
        "kind": "theme"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "The reviewer opened the durable rendered SOURCE screenshots for the PDP and collection and compared them against the clone side by side. A worker description, DOM inventory, or metrics-only review does not satisfy this criterion.",
        "product/default/index.liquid leads with the scaffold's canonical product-data section (product_hero or main_product — unmodified, not a hand-rolled replacement) and adds source-matching sections after it; collection/default exists and renders.",
        "At least one REAL product PDP and one collection route were loaded in the dev preview this turn without server/Liquid errors, with screenshots (or, when the catalog is legitimately empty for this run scope, the empty state renders cleanly and is noted).",
        "Every PDP or collection landmark recorded as video renders a real `<video>` from its DAM mapping at desktop and mobile; `read_preview_dom` verifies the element, URL, and playback attributes. Static-image, poster-only, gradient, and flat-color substitutions fail QA.",
        "The exact materialized `<THEME_AUDIT_PATH>` returned by run_skill passes on the touched files.",
        "Authored copy obeys `brand.md`. Mist injects the company's brand guide into every turn as a `<brand_voice>` block; scraped source strings win, and any string the agent had to write itself is in that voice and consistent with its Do's and Don'ts. Surviving base-theme placeholder copy — \"NEW DROP\", \"Built different\", \"Trusted by thousands\", \"Shop the drop\", lorem — is a defect even when the layout matches, and is the single most visible way a clone reads as generic.",
        "HARD-FAIL BAR: fail for erroring PDP/collection routes, a missing/substituted priority video, a replaced/modified main_product section, or unfixed audit violations. Other fidelity gaps vs the source PDP layout are refine-pass NOTES."
      ],
      "dependsOn": ["theme-homepage", "products-import"],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "theme-content-pages-push",
      "name": "Theme 6/6 — content pages, full audit & push",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop",
        "requiredTools": [
          {
            "tool": "screenshot_preview",
            "minSuccessfulCalls": 3,
            "distinctBy": ["path"]
          },
          {
            "tool": "read_preview_dom",
            "minSuccessfulCalls": 3,
            "distinctBy": ["path"]
          },
          {
            "tool": "read_preview_console"
          },
          {
            "tool": "read_local_server_logs"
          }
        ]
      },
      "prompt": "Finish and ship the theme. Call run_skill(\"themes/theme-clone\") for Phases 5-7 and the exact materialized `<THEME_AUDIT_PATH>`.\n\n1. Content page templates: from clone-manifest.json's page manifest (max 4 key static pages — about/faq/contact/policies), build `page/<slug>/index.liquid` templates with the scraped copy + DAM images. (Page RESOURCE records are created later by the content-import step — templates only here.)\n2. Audit gate: run `python3 <THEME_AUDIT_PATH>` on every file this clone TOUCHED (new or modified — check `git status`/your step notes) — fix every violation there. A fresh theme does not contain the skill's script. Then run the materialized script across the whole theme for visibility: the stock base-theme scaffold currently ships with ~100+ pre-existing violations in canonical sections you never touched; do NOT try to fix those (that's upstream base-theme work) — count them and record the per-rule rollup as a note in remaining_for_refine.\n3. Push: `fluid theme push` from the theme dir. API fallback gotchas (from live runs): a fresh company ships a provisioned active 'Base' theme; POST /api/application_themes with status:\"active\" 500s (RecordNotUnique) while another active theme exists — create WITHOUT status, upload resources, then PATCH {application_theme:{status:\"active\"}} to activate (honoring `theme_target_resolved` from clone-manifest.json — `existing` pushes to that theme_id). If the CLI push fails, fall back to the resource API per the skill.\n4. Verify after push: dev preview still healthy (screenshot_preview), and GET /api/application_themes confirms the landing theme exists; record its id. Then verify EVERY route type on the LIVE storefront in a real browser: home, one collection, one real product PDP (follow a link the storefront renders — do NOT trust the API's canonical_url, its slug goes stale after title PATCHes), and each content page. STALE-TEMPLATE GOTCHA (hit on every live run): a route whose MAIN region renders empty, or that falls through to the home template, has a stale application_theme_template record — resource PUTs around theme creation do not reliably re-expand them. Fix deterministically: find the record in GET /api/application_themes/{id} (match themeable_type + name), DELETE /api/application_theme_templates/{tpl_id}, re-PUT the matching template resource, wait ~10s, re-check. Applies to home_page, product, collection, and page templates alike.\n5. Aggregate every 'refine note' recorded by theme steps 1-5 plus your own into a single `remaining_for_refine` list.\n\nEnd with STEP_OUTPUT: theme id, pages/templates shipped, audit summary (0 violations), push result, and the aggregated remaining_for_refine list the refine step will consume.",
      "target": {
        "type": "kind",
        "kind": "theme"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "The exact materialized `<THEME_AUDIT_PATH>` returned by run_skill exits 0 on every file the clone touched (new/modified templates, sections, configs). Pre-existing violations in UNTOUCHED base-theme scaffold files are recorded as a note with the per-rule rollup — they are not this step's failure.",
        "The theme was pushed: GET /api/application_themes shows the landing theme and STEP_OUTPUT records its id (verify via the API, not the worker's claim).",
        "Every route type (home, collection, one real PDP followed from a storefront-rendered link, each content page) renders its own template's content in a real browser after the push — no empty main regions, no home-template fallbacks (stale application_theme_template records were refreshed via DELETE + re-PUT where needed).",
        "For every priority landmark recorded as video, the live storefront DOM contains the expected `<video>` with a working Fluid DAM source and source-matching playback behavior at desktop and mobile. A still/poster/color fallback does not count as shipped.",
        "Key static page TEMPLATES from the manifest exist (up to 4); STEP_OUTPUT carries the aggregated remaining_for_refine list.",
        "Authored copy obeys `brand.md`. Mist injects the company's brand guide into every turn as a `<brand_voice>` block; scraped source strings win, and any string the agent had to write itself is in that voice and consistent with its Do's and Don'ts. Surviving base-theme placeholder copy — \"NEW DROP\", \"Built different\", \"Trusted by thousands\", \"Shop the drop\", lorem — is a defect even when the layout matches, and is the single most visible way a clone reads as generic.",
        "HARD-FAIL BAR: fail when the push did not land (no theme id verifiable via the API), any priority video is absent/substituted, audit violations remain, or the preview is broken after push. A shorter-than-hoped page list or a long remaining_for_refine list is fine — the refine pass exists for that."
      ],
      "dependsOn": ["theme-product-collection"],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "iterative-theme-refine",
      "name": "Iterative screenshot-based theme refine",
      "model": "anthropic/claude-opus-5",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "stop",
        "requiredTools": [
          {
            "tool": "view_project_image",
            "minSuccessfulCalls": 6,
            "distinctBy": ["path"]
          },
          {
            "tool": "screenshot_preview",
            "minSuccessfulCalls": 6,
            "distinctBy": ["path", "width"]
          },
          {
            "tool": "read_preview_dom",
            "minSuccessfulCalls": 3,
            "distinctBy": ["path"]
          },
          {
            "tool": "read_preview_console"
          },
          {
            "tool": "read_local_server_logs"
          }
        ]
      },
      "skill": "themes/iterative-theme-refine",
      "target": {
        "type": "kind",
        "kind": "theme"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "Preflight validated all six durable source files from `clone-manifest.json`: each `.mist-desktop/source-baselines/` path exists, its `git hash-object --no-filters -- <path>` digest matches the recorded sha256, and its nonzero bytes/dimensions/requested viewport/final URL/status/overlay handling are complete. Missing or opaque source evidence was recaptured before any visual round.",
        "The reviewer opened the source and clone pixels side by side for every home/shop/PDP desktop/mobile pair. Worker summaries, DOM inventories, and metrics without the source image do not satisfy visual review.",
        "Phase A CODE-parity gate ran FIRST and reported CODE_PARITY: pass before any screenshot round: per-template structural comparison vs the source (sections present + ordered), brand tokens (no hardcoded hex/fonts), lint clean (no Liquid errors in the dev server log), every template route renders, and COPY/VOICE parity against `brand.md` (the `<brand_voice>` block) with no surviving base-theme placeholder copy. Screenshots are the granular FINAL pass — a run that started pixel-diffing before code parity does not satisfy this step.",
        "The skill ran up to five semantic source-vs-clone rounds over home, shop, and PDP at desktop and mobile, and reported STATUS: pass. `cap-reached` is an honest needs-review outcome, not acceptance for the flagship workflow.",
        "The final same-round evidence shows zero majors on all three priority routes, at most two itemized minors per route, no missing/reordered landmarks, correct copy/data/primary imagery, and priority landmark geometry within 5% unless responsive reflow is explicitly demonstrated.",
        "STEP_OUTPUT names every route and viewport verified and includes current screenshot + metrics paths generated after the final code change.",
        "The theme's dev preview is still healthy, every touched file passes theme_audit.py, and the final desktop/mobile routes have no Liquid errors or horizontal document overflow.",
        "HARD-FAIL BAR: fail when the loop never ran, the preview is broken, evidence is stale/fabricated, any priority route retains a major, or the run ends `cap-reached`."
      ],
      "dependsOn": ["theme-content-pages-push"],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "content-import",
      "name": "Import every page as a resource (collections, categories, pages, posts, menus)",
      "model": "google/gemini-3.6-flash",
      "qa": {
        "enabled": true,
        "model": "moonshotai/kimi-k3",
        "strictness": "standard",
        "onFail": "continue"
      },
      "prompt": "First call `run_skill(\"fluid-product-admin-import\")` and read the loaded skill plus every referenced file. Then create every in-scope non-product storefront resource represented by the source and the theme's clone-manifest; use the loaded contract and query_docs before any schema you are not certain about.\n\nFIRST: if `import_products` is not true, emit STEP_OUTPUT { skipped:true, reason:\"content out of scope\" } and return.\n\nReconcile sitemap URLs + clone-manifest page inventory into immutable source identities. Use GET-before-write and persisted mappings so restart or a second run creates no duplicates:\n- Collections: `/api/v202604/company/collections`; preserve source lifecycle and membership. `product_ids` is a full replacement set, so omit it until the complete mapped set is known.\n- Categories: `/api/v202604/company/categories`, only when the source has a distinct curated taxonomy. Create parents first.\n- Static pages: use Mist's `create_page` tool for each visible page so the v202604 page resource, theme template, local route, and preview pane are created together. The theme push already ran; never bypass `create_page` with a legacy page endpoint.\n- Posts: `/api/v202604/company/posts`; preserve only documented fields, source date/lifecycle, custom slug intent, and DAM-hosted SEO/hero media.\n- Playlists/media: use `/api/v202604/company/playlists` and `/api/v202604/company/media` when the source actually has those resource types. The playlist request wrapper is intentionally `library`.\n- Menus: `/api/menus` is the explicit legacy exception. Build header/footer navigation from canonical URLs returned by successful destination writes; never compose guessed slugs.\n\nUse `status:\"published\"` where the documented resource lifecycle supports it. Never use `/api/company/v1/*`, `/api/company/pages`, or `/api/posts` as fallbacks. Cursor-paginate each v202604 list through `meta.pagination.next_cursor === null`; do not expect `total_count`.\n\nAfter writes, verify every source identity against the complete destination listing, open each new page in the local preview pane, and check that its expected content/template—not merely a 200 response—renders. End with STEP_OUTPUT: { collections:{source,created,mapped}, categories:{...}, pages:{source,created,previewed}, posts:{...}, playlists:{...}, media:{...}, menus:{...}, destination_pages_by_type, idempotent_second_pass, skipped_junk:[], failures:[], remaining_for_human:[] }.",
      "target": {
        "type": "manager"
      },
      "runIf": {
        "flag": "import_products"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "If import_products was not selected, the step correctly skipped and did no work.",
        "Otherwise: every source collection, every curated category (or justified none), every static /pages/* page, every published post, every source playlist/media resource, and the site nav/menus were mapped to the correct Fluid resource type. v202604 lists were cursor-paginated to next_cursor=null and exact source-vs-created counts are reported without relying on total_count.",
        "All visible static pages were created through Mist create_page and opened in the local preview pane; verification asserts expected page content/template, not merely HTTP 200.",
        "Creates are idempotent (GET-before-POST, no duplicates on re-run); legitimately-empty types (e.g. no blog articles) are justified, not fabricated.",
        "STEP_OUTPUT reports per-type source-vs-created counts and any remaining_for_human items.",
        "HARD-FAIL BAR: fail when a source resource is silently missing, a documented v202604 type fell back to a legacy endpoint, pages were created without create_page/preview verification, duplicates were created, or counts were fabricated. A named per-item platform failure may remain for human review but cannot be reported as complete."
      ],
      "dependsOn": ["gather-context-qa", "theme-content-pages-push"]
    },
    {
      "id": "populate-getting-started",
      "name": "Reconcile onboarding info",
      "model": "google/gemini-3.6-flash",
      "qa": {
        "enabled": true,
        "model": "moonshotai/kimi-k3",
        "strictness": "standard",
        "onFail": "continue"
      },
      "prompt": "Reconcile the company's server-side onboarding record with what this run actually produced. NOTE: the legacy checklist endpoints (`/api/v1/onboardings`, `check_step`, `complete_step`) are REMOVED from api.fluid.app — they 404. Do NOT chase them. The live surface is:\n\n1. Resolve the active company id (GET /api/me → company.id, or /api/settings/companies).\n2. GET /api/companies/{id}/onboarding_info — read what's on file (country_code, countries_info, entity, settlement currency, business fields).\n3. Cross-check against the gather-context step's output. For any HIGH-confidence field that is empty on the record, deep-merge it into the full GET result and PUT `/api/companies/{id}/onboarding_info` with `{onboarding_info:<merged>}`. PUT replaces the blob; never send a partial blob, overwrite a non-empty value, or push LOW-confidence data. Use the live schema keys exactly: `refund_or_return_policy`, `sells_health_supplements`, and inverse `claims_no_diseases` plus specific `claims_*_treatment` flags — never the silently dropped aliases `refund_policy`, `sells_supplements`, or `makes_disease_claims`. Re-GET and verify every write survived serialization. KYC/payments fields that require human/legal input are ALWAYS deferred, never fabricated.\n4. Report: fields already set, fields this step filled, fields deferred to the human with reasons ('requires payments KYC', 'requires legal entity docs', 'human decision').\n\nThis step is best-effort reporting — an honest 'endpoint returned <status>, nothing writable' report with the exact request/response is a VALID outcome. End with STEP_OUTPUT: { onboarding_info_read: true|false, fields_set:[...], fields_filled:[...], deferred:[{field,reason}], endpoint_notes:[...] }.",
      "target": {
        "type": "manager"
      },
      "runIf": {
        "flag": "push_business_data"
      },
      "maxReworkRounds": 1,
      "recovery": { "mode": "manual" },
      "acceptance": [
        "The step made a real GET /api/companies/{id}/onboarding_info call this turn (or documented its exact failure status) — the report cites concrete fields from the actual response, and does NOT call the removed /api/v1/onboardings endpoints.",
        "No non-empty server value was overwritten; nothing LOW-confidence or KYC-sensitive was pushed; every deferred field has an explicit reason.",
        "STEP_OUTPUT follows the documented shape with fields_set / fields_filled / deferred.",
        "HARD-FAIL BAR: fail only for fabrication (claiming reads/writes the API doesn't show) or destructive overwrites. 'Read-only outcome, everything deferred to the human' is a PASSING outcome when reported honestly."
      ],
      "dependsOn": ["products-import", "gather-context-qa"]
    },
    {
      "id": "ugc-discovery",
      "name": "Discover TikTok / UGC content for the DAM",
      "model": "google/gemini-3.6-flash",
      "qa": {
        "enabled": true,
        "model": "moonshotai/kimi-k3",
        "strictness": "standard",
        "onFail": "continue"
      },
      "prompt": "FIRST: if context flag `discover_ugc` is not true, emit STEP_OUTPUT { skipped:true, reason:'discover_ugc not selected' } and return. NOTE: this is OTHER PEOPLE's content about the brand (UGC); the brand's OWN channel content is the separate brand-social-import step. Otherwise proceed.\n\nFind user-generated and social content for this company and load it into the Fluid DAM. This step is best-effort — a company with no discoverable social presence is a valid outcome.\n\n1. From the gather-context step's output, pull TikTok / Instagram handles. If none were found there, run one social_search (platform tiktok) with the company name as a keyword to double-check.\n2. If no handles and no keyword hits: emit a short 'no discoverable UGC presence — queries attempted: [...]' report and return. This is acceptance, not failure.\n3. Otherwise, for 3-8 promising videos, use the video_ripper tool to download and dam_upload to push into the company's DAM. Tag uploads with the brand name from gather-context.\n4. Report every DAM asset URL you created.",
      "target": {
        "type": "manager"
      },
      "acceptance": [
        "Either at least one UGC asset was uploaded to the DAM (verifiable via its reported URL), or an explicit finding documents no discoverable social/UGC presence with the queries tried.",
        "No unrelated or off-brand content was uploaded."
      ],
      "dependsOn": ["populate-getting-started"],
      "runIf": {
        "flag": "discover_ugc"
      }
    },
    {
      "id": "brand-social-import",
      "name": "Import the brand's own YouTube + TikTok content",
      "model": "google/gemini-3.6-flash",
      "qa": {
        "enabled": true,
        "model": "moonshotai/kimi-k3",
        "strictness": "standard",
        "onFail": "continue"
      },
      "prompt": "FIRST: if context flag `import_brand_social` is not true, emit STEP_OUTPUT { skipped:true, reason:'import_brand_social not selected' } and return (passes QA as a correct skip). Otherwise follow the `marketing/brand-social-import` skill exactly.\n\nImports the COMPANY'S OWN social video content — NOT UGC. (1) Resolve the company's official YouTube + TikTok accounts from gather-context social links / the site footer (confirm via social_search platform:tiktok user-mode only if unlinked — never import an unconfirmed account); (2) register confirmed handles via PATCH /api/settings/social_media (GET first, don't clobber); (3) enumerate each account's videos (TikTok: social_search platform:tiktok mode:user; YouTube: social_search platform:youtube mode:user with the handle or UC… channel id); (4) rip each via video_ripper (DAM + Media in one call) tagged `brand-content,<platform>,<brand>` — idempotent, skip already-imported, never abort on one failure. Report accounts registered, videos found/ripped/skipped/failed with DAM URLs.",
      "target": {
        "type": "manager"
      },
      "runIf": {
        "flag": "import_brand_social"
      },
      "acceptance": [
        "If import_brand_social was not selected, the step correctly skipped (STEP_OUTPUT skipped:true) and did no work.",
        "Otherwise: official accounts were resolved (site links, or search-confirmed — no unconfirmed accounts) and registered via PATCH /api/settings/social_media.",
        "Otherwise: each account's videos were ripped into DAM + Media tagged `brand-content` (never `ugc`), idempotently, per-video failures noted without aborting; or an explicit finding documents no discoverable official social presence.",
        "No UGC / other-people's content was imported by this step (that is ugc-discovery)."
      ],
      "dependsOn": ["gather-context"]
    },
    {
      "id": "verify-links",
      "name": "Verify links resolve to the right resource templates",
      "model": "moonshotai/kimi-k3",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "continue"
      },
      "dependsOn": ["iterative-theme-refine", "products-import"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Audit EVERY link in the storefront and make each point to a valid, correctly-rendering Fluid resource template — this is the step that catches nav items wired to dead or generic routes. Cover: the header navigation, the footer (support / legal / social / localization), and primary in-page CTAs (hero, category tiles, banners). For each link, resolve the target and confirm it (a) returns HTTP 200 and (b) renders the CORRECT template: collection links -> collection template, category -> category, page -> page, product -> /home/products/<slug>, home -> home. FIX links that point to: a dead or generic route (e.g. a bare `/shop` that 204s instead of `/home/shop`), the wrong template, a Shopify-style URL that doesn't map to Fluid's `/home/...` routes, or a 302/404/406. EVERY resource-DETAIL href the theme emits MUST carry the credit segment (`/home/products/<slug>`, `/home/collections/<slug>`, `/home/pages/<slug>`, `/home/posts/<slug>`) — a bare `/products/<slug>` 404s at the origin and only appears to work once the CDN has cached the `/home/...` render, then breaks again when that cache entry ages out. Test each link COLD: if a bare detail URL returns 200, re-test it against a slug you have not visited this run before believing it. Also grep the rendered HTML for the unsubstituted credit placeholder `__fluid_affiliate_username__` — it resolves by fallback but must be replaced with `home` (or a real Liquid credit variable) in the theme source. Navigation menu items MUST map to real resources — e.g. 'By Scent' -> a collections/scent index (`/home/collections`), NOT a generic shop URL; 'Best Sellers' -> `/home/collections/best-sellers`; 'Gifts & Sets' -> `/home/collections/sets`. Prefer binding the nav to a real Fluid navigation menu (link_list) with correct resource URLs so it stays admin-manageable; otherwise correct the hrefs. Point footer info links at the imported pages (`/home/pages/<slug>`). A PDP (or any resource type) that returns 204/blank or 500 on its CREDIT-PREFIXED URL is NOT 'platform-side' — it almost always means the active theme has no `default: true` template for that themeable_type (check `GET /api/application_themes/active` -> application_theme_templates grouped by themeable_type). Fix the theme; do not wave it through. SLUG GOTCHA: enumerate PDP URLs from links the STOREFRONT renders (collection/search pages), not from the API's canonical_url — a PATCH that includes the title re-slugifies the product (new uuid suffix) and the old canonical_url silently falls through to the home template. End with STEP_OUTPUT containing a table: link label -> target URL -> HTTP status -> resolved template.",
      "acceptance": [
        "Every header-nav, footer, and primary in-page link resolves to HTTP 200 and the correct resource template, asserted on the credit-prefixed URL. A PDP/collection/page that 204s or 500s is treated as a theme template-default gap to fix, not a platform excuse.",
        "No link points to a dead/generic route (e.g. a bare `/shop`), a Shopify-style URL, or the wrong template type.",
        "No emitted resource-detail href is bare (`/products/<slug>`) or contains `__fluid_affiliate_username__`; every one carries the `/home/` credit segment.",
        "Navigation menu items map to real resources (By Scent -> a collections/scent index, Best Sellers -> the best-sellers collection, Gifts & Sets -> the sets collection), ideally via a bound Fluid navigation menu rather than hardcoded hrefs.",
        "STEP_OUTPUT includes the link-audit table (label -> target -> HTTP status -> resolved template)."
      ],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "product-price-qa",
      "name": "Product price QA",
      "model": "moonshotai/kimi-k3",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "stop"
      },
      "dependsOn": ["product-active-status-qa"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — PRODUCT PRICING ONLY. Use query_docs to confirm the v202604 Product list/show response before checking. Cursor-paginate `GET /api/v202604/company/products?page[limit]=100` through every `meta.pagination.next_cursor` until null; do not assume total_count or stop at the first 100.\n\nFor EVERY real published product, load its detail record as needed and visit its local-preview PDP using the canonical route returned/mapped by the API. Compare the rendered retail price and, when the source genuinely offers one, subscription price against the default company's country-relative Product pricing/variant-country data. A missing, zero, stale, wholesale, or mismatched value fails that product.\n\nCheckpoint per product so interruption/restart resumes rather than restarting the catalog. Keep preview navigations bounded (maximum two in flight) and report progress every 20 products to avoid overwhelming a lower-end machine or slow network.\n\nIf the company serves multiple countries, test every configured country on the flagship and one multi-variant product; confirm both amount and currency change to that country's resolved pricing. Do not check links, cart, or layout here.\n\nEnd with STEP_OUTPUT: { enumerated, checked, destination_pages, default_country, matches, mismatches:[{product_id,title,route,displayed,expected,reason}], country_switches:[...], checkpoint_path, unresolved:[] }.",
      "acceptance": [
        "Every cursor-paginated product was checked; each PDP's rendered retail price and currency exactly match the default-country v202604 response. Zero is accepted only when the source manifest also says the product is legitimately free.",
        "Subscription price is shown and matched only for products whose source and destination genuinely carry a subscription plan.",
        "For multi-country stores, every configured country was verified on the flagship and one multi-variant PDP (or the report proves the store is single-country).",
        "STEP_OUTPUT lists every product's price check, destination page count, checkpoint path, and zero unresolved mismatches."
      ],
      "runIf": {
        "flag": "import_products"
      }
    },
    {
      "id": "add-to-cart-qa",
      "name": "Add to cart QA",
      "model": "moonshotai/kimi-k3",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "stop"
      },
      "dependsOn": ["product-active-status-qa"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — ADD TO CART ONLY. Visit EVERY PDP and confirm the add-to-cart button is correctly wired through FairShare: `data-fluid-add-to-cart=\"<variantId>\"` is present and the id is a REAL active variant of that product; on multi-variant products it is VARIANT-AWARE (selecting a scent/size updates the button's variant id + price); `data-fluid-quantity` and `data-fluid-open-cart-after-add` are sane. Confirm the cart drawer/FairShare SDK is present (the `fluid-cdn-script` is injected). Flag any PDP with a missing, hardcoded, placeholder, or stale variant id, or a non-variant-aware selector. Do NOT check prices/links/layout. STEP_OUTPUT: a table of PDP -> data-fluid-add-to-cart value -> real active variant? -> variant-aware?, plus any failures.",
      "acceptance": [
        "Every PDP has a data-fluid-add-to-cart bound to a real, active variant id for that product.",
        "Multi-variant products update the add-to-cart variant id (and price) when a variant is selected.",
        "No PDP has a missing, placeholder, or stale variant id.",
        "STEP_OUTPUT lists every PDP's add-to-cart wiring and names any failure."
      ],
      "runIf": {
        "flag": "import_products"
      }
    },
    {
      "id": "mobile-responsive-qa",
      "name": "Mobile responsive QA",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop"
      },
      "dependsOn": ["iterative-theme-refine", "products-import"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — MOBILE ONLY. Use the fresh-machine Mist preview capabilities, not a local/global Playwright assumption. Call start_preview if needed, confirm local URL with preview_state, then for one of EACH route type — home, collection, category (if any), product PDP, content page, cart — call screenshot_preview with `{mode:\"full\",path:\"<route>\",width:390,height:844}`. Require HTTP 200 and use the reported document width to prove no horizontal overflow. Inspect images/text/tap-target geometry in the screenshots. On the home route, inspect the real mobile-menu button selector, call interact_preview to open it, take a fresh 390x844 screenshot, then close it and recapture; aria-expanded/state and visible menu content must change. Read preview console + local server logs after the route matrix. Flag any overflow, overlap/cutoff, broken mobile nav, unreadable type, or runtime failure. Do NOT check prices/cart wiring/link targets. STEP_OUTPUT: route -> status -> viewport/document width -> screenshot paths -> issues, plus mobile-menu interaction evidence and log status.",
      "acceptance": [
        "Every sampled route has current screenshot_preview evidence at an exact 390x844 viewport, HTTP 200, and reported document width <= 390px.",
        "The header navigation has a working mobile state proven by interact_preview plus fresh before/open/closed screenshots and an aria-expanded or equivalent visible state change.",
        "Primary CTAs/tap targets are adequately sized and images scale within the viewport.",
        "Preview console and local server logs are clean after the matrix, and STEP_OUTPUT includes each route's screenshot path, viewport/document width, status, and any flagged issue."
      ],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "product-active-status-qa",
      "name": "Product active-status QA",
      "model": "moonshotai/kimi-k3",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "stop"
      },
      "dependsOn": ["products-import"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — PRODUCT LIFECYCLE ONLY, before price and add-to-cart QA. Use query_docs for the v202604 list/update contract. Cursor-paginate ALL products from `GET /api/v202604/company/products?page[limit]=100` until `meta.pagination.next_cursor` is null.\n\nA launch product must resolve as raw `status:\"active\"` AND `active:true`. For any manifest-backed product that is draft, archived, or inactive by mistake, PATCH `/api/v202604/company/products/{id}` with only the documented lifecycle keys needed—normally `{product:{status:\"active\",public:true,publish_at:null}}`—then re-read it. `published` is a read-side presentation label on some surfaces, not a valid v202604 ProductWrite enum.\n\nAfter correction, verify every product's local PDP route renders the expected product rather than a 404/redirect and verify collection membership for products that should have it. Use returned canonical routes and checkpoints; do not compose slugs or stop at a page limit. Do not check price, cart, or visual layout here.\n\nEnd with STEP_OUTPUT: { enumerated, destination_pages, before_status_counts, corrected:[{id,from,to}], after_status_counts, pdp_routes_checked, membership_failures:[], unresolved:[], checkpoint_path }.",
      "acceptance": [
        "Every destination cursor was exhausted and every manifest-backed product resolves as raw `status == active` and `active == true`; no launch product remains draft, archived, or inactive.",
        "The same complete sweep confirms every source-non-subscription product has `has_subscription_plans == false` and no active/default product subscription join; any auto-attached company default was repaired through the documented nested v202604 update and re-read.",
        "Every product's local PDP route renders the expected product (not merely HTTP 200, a redirect, or a different page).",
        "No collection is left with only draft members.",
        "STEP_OUTPUT reports before/after status counts, every correction, destination page count, and zero unresolved lifecycle mismatches."
      ],
      "runIf": {
        "flag": "import_products"
      }
    },
    {
      "id": "storefront-route-health-qa",
      "name": "Storefront route health QA (browser-only)",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop"
      },
      "dependsOn": ["product-active-status-qa", "iterative-theme-refine"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — ROUTE HEALTH. Assert the home index AND one of EACH resource type — collection, category (if any), product PDP, content page, cart — returns HTTP 200 with a non-trivial rendered body.\n\nURL shape is part of the test. Fluid detail routes require `/{credit}/{resource}/{slug}`, with `home` as unattributed credit: `/home/products/<slug>`, `/home/collections/<slug>`, `/home/pages/<slug>`, `/home/posts/<slug>`. Index routes are credit-optional. Never accept a bare Shopify-style detail URL; a cached 200 there is not routing proof.\n\nPRECHECK 1: GET `/api/application_themes/active`; compare its id to the built theme id. If different/draft, activate the built theme before continuing or report a blocker.\n\nPRECHECK 2: group active theme templates by themeable_type and require a `default:true` template for layouts, locales, config, home_page, navbar, footer, product, collection, category, page, post, cart_page, shop_page. Fix missing defaults; do not label blank 204 routes as platform bugs.\n\nMETHODOLOGY: use Mist's managed browser. For each PUBLIC storefront URL, call crawl with `formats:[\"html\",\"screenshot\"]`, `only_main_content:false`, and exact viewport screenshot options. Record requested URL, final URL, metadata HTTP status, title, screenshot, and non-trivial resource-specific body. A redirect to home, generic home body, blank/loading screenshot, 204, 404, or 500 fails. For the matching LOCAL route, call screenshot_preview with path + exact viewport and record its navigation status. Read preview console/local logs. This requires no local Playwright install. If an older Mist build cannot report crawl final URL/status or exact screenshots, report tooling needs-review rather than inventing a browser pass.\n\nSTEP_OUTPUT: route type -> public requested/final URL -> public status/rendered -> local URL/status -> evidence paths; plus PRECHECK 1 active/built theme ids and PRECHECK 2 default-template table.",
      "acceptance": [
        "PRECHECK 1 ran: the theme returned by GET /api/application_themes/active is the theme this run built (or the drift is reported as a blocker).",
        "PRECHECK 2 ran: every required themeable_type (layouts, locales, config, home_page, navbar, footer, product, collection, category, page, post, cart_page, shop_page) has a default:true template, or the missing ones are listed and fixed.",
        "Every sampled route type (home, collection, category, PDP, content page, cart) is asserted on the credit-prefixed public URL where required and has managed-browser evidence: requested URL == intended final route, HTTP 200, non-trivial resource-specific HTML, and a non-blank current screenshot; the matching local preview route also reports a healthy navigation status.",
        "No route is declared healthy on the strength of a bare Shopify-style URL (`/products/<slug>`, `/collections/<slug>`) — those are cache-dependent and not a supported Fluid path.",
        "STEP_OUTPUT reports the per-route-type browser status plus both precheck tables."
      ],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "seo-metadata-qa",
      "name": "SEO metadata QA",
      "model": "moonshotai/kimi-k3",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "continue"
      },
      "dependsOn": ["storefront-route-health-qa"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — SEO METADATA ONLY (browser-based). For each route type (home, a collection, a product PDP, a content page), load it in a real browser and assert the <head> + document are SEO-ready: a non-empty, resource-SPECIFIC <title> (NOT empty and NOT a generic storefront default like 'Live Free.'), a meaningful <meta name=description> (not the generic default), a <link rel=canonical>, and exactly ONE <h1> with real resource content. Product and collection pages should also emit the relevant JSON-LD (Product / ItemList / BreadcrumbList). Fix what the THEME controls (title/description/canonical/h1/JSON-LD in the layout + templates, driven by the resource drop — e.g. `{{ page_title }}`, product/collection fields). FLAG what only store-level SEO settings can fix (site-wide default title/description, robots) for the human. STEP_OUTPUT: a per-route-type table -> title / description / canonical / h1 / JSON-LD present? -> pass/flag.",
      "acceptance": [
        "Every route type has a non-empty, resource-specific <title> (not a generic default) and a meaningful meta description.",
        "Every route type has a <link rel=canonical> and exactly one <h1> with real content.",
        "Product and collection pages emit the relevant JSON-LD.",
        "Theme-controllable gaps are fixed; store-level SEO-setting gaps are flagged for the human. STEP_OUTPUT lists the per-route SEO check.",
        "Each PDP <title> contains the product name; if Fluid's layout scope makes product unavailable so the title falls to the brand default, flag it for the human rather than leaving it silently generic."
      ],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "page-route-content-qa",
      "name": "Page route content QA",
      "model": "moonshotai/kimi-k3",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "strict",
        "onFail": "continue"
      },
      "dependsOn": ["storefront-route-health-qa"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — CONTENT PAGE ROUTES ONLY. Use query_docs for the v202604 Page list/show contract, then cursor-paginate `GET /api/v202604/company/pages?page[limit]=100` until `meta.pagination.next_cursor` is null.\n\nFor every source-authored content page (About, Contact, Shipping, Privacy, Terms, FAQ, store locator, and every other manifest page), open its local preview route in a real browser and assert the expected authored main content renders—not just shared header/footer chrome and not a different page behind a redirect. Use the canonical path returned/mapped by the page/create_page result; never compose a slug.\n\nIf a page resource or theme template is missing, repair it with Mist's `create_page` tool so resource, template, route, dev server, and preview pane remain coordinated. Do not raw-POST a legacy page endpoint. Re-open the page after repair and verify every header/footer link that targets it resolves to the same authored content.\n\nEnd with STEP_OUTPUT: { enumerated_pages, destination_pages, rows:[{source_url,title,canonical_path,record_exists,template_exists,body_renders,linked_from_resolves,status}], repaired_with_create_page:[], unresolved:[] }.",
      "acceptance": [
        "Every authored content page renders its real body copy in a browser, not just nav + footer.",
        "Missing page records/templates are repaired via Mist create_page and re-verified in the local preview pane; no legacy raw page endpoint is used.",
        "Every footer/nav link to a /pages/ route resolves to a page with real content.",
        "STEP_OUTPUT lists per-page record-exists / body-renders / linked-from status."
      ],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "font-substitution-qa",
      "name": "Font substitution QA",
      "model": "moonshotai/kimi-k3",
      "qa": {
        "enabled": true,
        "model": "openai/gpt-5.6-sol",
        "strictness": "standard",
        "onFail": "continue"
      },
      "dependsOn": ["storefront-route-health-qa"],
      "target": {
        "type": "manager"
      },
      "maxReworkRounds": 2,
      "recovery": { "mode": "manual" },
      "prompt": "Focused QA — FONT LICENSING & SUBSTITUTION ONLY. Determine the source brand's primary display + body typefaces. If a source font is proprietary (not OFL/Google-hosted/freely licensable) and was substituted with a free alternative, assert the substitution is done honestly and completely: (1) the closest free family is used consistently across the theme (no lingering references to the proprietary family name), (2) a license disclaimer naming the original font and the free substitute is embedded in the theme (e.g. a comment in layouts/theme.liquid), and (3) a sane font-stack fallback is present so text never renders in an unstyled default. If the brand's own licensed font files ARE importable (this is their store), import the real @font-face and keep the disclaimer noting the licensing assumption. STEP_OUTPUT: source font(s) -> proprietary? -> substitute used -> disclaimer present? -> fallback present? -> pass/flag.",
      "acceptance": [
        "Source typefaces identified; any proprietary font that was substituted is called out.",
        "Substitute family is applied consistently with no leftover proprietary-family references.",
        "A license disclaimer (original + substitute) is embedded in the theme and a fallback stack is present.",
        "STEP_OUTPUT lists per-font substitution + disclaimer + fallback status."
      ],
      "runIf": {
        "flag": "build_theme"
      }
    },
    {
      "id": "launch-readiness-review",
      "name": "Launch-readiness review",
      "model": "openai/gpt-5.6-sol",
      "qa": {
        "enabled": true,
        "model": "anthropic/claude-opus-5",
        "strictness": "strict",
        "onFail": "stop"
      },
      "prompt": "Produce a live-verified launch-readiness report. This is NOT a summary of what earlier steps said — every claim here must be backed by a check YOU performed this turn.\n\n1. **Storefront live check** — from the gather-context step's context, find the company's public storefront URL (from /api/settings/companies/{id}). crawl(<url>, formats: ['screenshot']) and confirm it returns without error. If that crawl returns a SCRAPE_TIMEOUT, retry ONCE with a plain/markdown crawl (crawl(<url>) with no screenshot format — it succeeds when the screenshot renderer times out) and treat a clean plain crawl as the storefront being live BEFORE you record a storefront miss. Attach the screenshot's URL (or, on the fallback, the plain-crawl result) to the report.\n2. **Theme sanity** — screenshot_preview once to confirm the theme's dev preview still renders. Read the iterative-theme-refine step's STEP_OUTPUT for any remaining majors/minors and surface them.\n3. **Products** — query_docs the v202604 response, then cursor-paginate `GET /api/v202604/company/products?page[limit]=100` through `meta.pagination.next_cursor === null`; count the accumulated unique IDs. Reconcile that complete set with products-import's source manifest and status/price QA outputs. Spot-check at least the flagship, one multi-variant product, and one deterministic seeded-random product for title, country-relative price/currency, DAM image, variants, raw `status:active`, and `active:true`. Flag any Sample Product stub.\n4. **Onboarding info** — GET /api/companies/{id}/onboarding_info (the legacy /api/v1/onboardings endpoints are removed — do not call them) and record what business/country info is on file vs still missing. Compare to what populate-getting-started reported; flag drift. CLASSIFY missing fields, do not blanket-blocker them: a field populate-getting-started listed as `deferred` with a human-input reason (KYC/payments/legal-entity — these ALWAYS need a human) is an EXPECTED gap, so surface it as a `medium`/human-action item, NOT a launch blocker. An empty `onboarding_info: {}` is therefore not automatically unlaunchable — only treat a missing field as a `high`/`blocker` when it should have been filled this run but is absent because a scrape or earlier step FAILED (not merely deferred). Reserve `blocker` for fields that truly block going live.\n5. **Brand + business info** — GET /api/settings/brand_guidelines and confirm source-matched colors, DAM identity images, non-boilerplate `brand_md`, and every legally re-hostable `fonts[]` entry landed with its exact DAM URL/metadata. Confirm proprietary or unverified source-font bytes were not copied and the licensed substitute is documented. Flag any drift.\n6. **Degraded steps** — if any earlier step finished in a degraded/needs-review state (its findings appear in your context), fold its unresolved findings into the checklist below with appropriate severity.\n7. **Prioritized checklist** — assemble a `Before you launch:` list. Each entry has:\n   - severity: `blocker` (unlaunchable), `high` (embarrassing at launch), `medium` (fix week 1), `low` (nice-to-have)\n   - what: one line\n   - why: which check this turn produced it\n   - suggested action\n\nRank by severity. If there are zero blockers and ≤ 2 highs, say so plainly — the report is not required to invent problems.",
      "target": {
        "type": "manager"
      },
      "acceptance": [
        "The report cites live checks performed THIS turn: a complete cursor traversal of GET /api/v202604/company/products, at least one GET /api/companies/{id}/onboarding_info (never the removed /api/v1/onboardings), GET /api/settings/brand_guidelines including fonts/brand_md verification, and either a crawl-screenshot of the public storefront or an explicit 'storefront URL not yet published' note.",
        "The 'Before you launch' checklist exists and every entry has severity + what + why + suggested action.",
        "Any remaining majors from iterative-theme-refine's STEP_OUTPUT (and any degraded/needs-review step findings present in context) appear in the checklist with severity assigned.",
        "The manual-stub product 'Sample Product' is either flagged as a blocker (Case C ran) or explicitly noted as absent (Case A/B ran).",
        "Missing onboarding_info fields are classified, not blanket-blockered: KYC/payments/legal fields populate-getting-started deferred to a human are surfaced as human-action items (not launch blockers), while fields absent due to a scrape/step failure are flagged with real severity. An empty onboarding_info is not, by itself, treated as unlaunchable."
      ],
      "dependsOn": [
        "add-to-cart-qa",
        "mobile-responsive-qa",
        "product-active-status-qa",
        "product-price-qa",
        "seo-metadata-qa",
        "storefront-route-health-qa",
        "ugc-discovery",
        "verify-links",
        "page-route-content-qa",
        "font-substitution-qa",
        "content-import",
        "gather-context-qa",
        "populate-getting-started"
      ]
    }
  ]
}
